import React from "react";
import Map from "../components/map";

const MapPage = () => {
    return (
        <div>
            <p>Click anywhere!</p>
            <Map />
        </div>
    );
};

export default MapPage;
